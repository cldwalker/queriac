<h1 class="inline"><%= @tag.blank? ? "All Commands" : "Commands tagged with #{@tag.gsub(' ', ' and ')}" %></h1>

<ul class="bolder options header">
	<li class="add"><%= link_to 'New command', new_command_path %></li>
</ul>

<hr />

<table class="top_spaced">
  <tr>
    <th>Name</th>
    <th width="70">Keyword</th>
    <th width="30" class="centered">Hits</th>
    <th>Last Used</th>
		<th>Options</th>
  </tr>
 
<% for command in @commands %>
	<tr <%= cycle('class="offset"', '') -%>>	
    <td class="<%= command.kind %>">
			<p class="command_title"><%= link_to command.name, command.show_path %></p>
			<p class="tag_list">Tags: <%= command.tags.map{|t| link_to(t.name, "/#{command.user.login}/commands/tag/#{t.name}") }.join(" ") %></p>
		</td>
    <td class="centered"><%= command.keyword %></td>
    <td class="centered <%= "faded" if command.queries.size == 0 %>"><%= command.queries.size %></td>
    <td><%= command.queries.size > 0 ? time_ago_in_words_or_date(command.queries.last.created_at) : link_to("(Unused)", command.show_path, :class => "faded") %></td>	
		<td>
		<% if logged_in? %>
			<ul class="options">
				<% if owner? command %>
					<li class="no_icon"><%= link_to image_tag("icons/edit.png"), command.edit_path %></li>
					<li class="no_icon"><%= link_to image_tag("icons/delete.png"), command_path(command), :confirm => "Are you sure you want to delete this command?", :method => :delete  %></li>
				<% else %>
					<li class="add"><%= link_to 'Copy', new_command_path({:ancestor => command.id}) %></li>
				<% end %>				
			</ul>
		<% end %>
		</td>
  </tr>
<% end %>
</table>

<%= will_paginate @commands unless @tag %>